<?php
/**
 * Pencil overlay template — currently a no-op stub.
 *
 * The pencil overlay's DOM is built dynamically by pencil.js. This
 * template exists as a hook point for future server-rendered content
 * (e.g., a search index of theme components).
 */
if ( ! defined( 'ABSPATH' ) ) exit;
// Intentionally empty.
